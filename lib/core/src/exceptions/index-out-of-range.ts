import { Exception } from './exception';

/**
 * Thrown when an attempt is made to access an element using an index is outside the list or array's boundaries.
 */
export class IndexOutOfRange extends Exception {}
